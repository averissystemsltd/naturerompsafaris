# Copy this file to `.env.local`. Add SUPABASE_SERVICE_ROLE_KEY from Supabase Dashboard before Vercel import.

NEXT_PUBLIC_SITE_URL=https://naturerompsafaris.com
NEXT_PUBLIC_PORTAL_URL=https://portal.naturerompsafaris.com
NEXT_PUBLIC_PORTAL_HOST=portal.naturerompsafaris.com

# Supabase — required for portal + contact form
NEXT_PUBLIC_SUPABASE_URL=https://aboxocmprjtkprzcvxns.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Localization
NEXT_PUBLIC_DEFAULT_LOCALE=en
NEXT_PUBLIC_SUPPORTED_LOCALES=en,sw,fr,de,es,it,zh

# Manual translation backfill (Google Cloud Translation API — server-only, set in Vercel)
# Enable Cloud Translation API in Google Cloud Console, create an API key restricted to that API.
# Translations run only from Portal → Settings → Manual Translation (not on publish).
GOOGLE_TRANSLATE_API_KEY=
AUTO_TRANSLATE_ENABLED=false
# Comma-separated target locales (defaults to all supported locales except en)
AUTO_TRANSLATE_LOCALES=sw,fr,de,es,it,zh
# Set to false to save translations as drafts (published_at null) for human review in the portal
AUTO_TRANSLATE_AUTO_PUBLISH=true

# Search indexing on publish
# IndexNow — Bing, Yandex, and partners (key file at /{INDEXNOW_API_KEY}.txt)
INDEXNOW_API_KEY=naturerompsafaris-indexnow
# Google Indexing API — paste the full service-account JSON (or base64 of it).
# 1) Enable "Web Search Indexing API" in Google Cloud
# 2) Create a service account + JSON key
# 3) Add the service account email as Owner in Search Console for naturerompsafaris.com
# Without this, Google still discovers URLs via the sitemap; IndexNow still runs.
GOOGLE_INDEXING_SERVICE_ACCOUNT_JSON=

# Sentry — set DISABLED=false and fill DSN/org/project when you create a Sentry project
NEXT_PUBLIC_SENTRY_DISABLED=true

# Docker/standalone output (false for Vercel)
BUILD_STANDALONE=false

# Cloudflare Turnstile (bot protection on enquiry forms — free at dash.cloudflare.com)
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# Outbound email — Nature Romp mail host. Use port 465 + SMTP_SECURE=true (SSL),
# or port 587 + SMTP_SECURE=false (STARTTLS). Each mailbox can have its own
# password; SMTP_PASSWORD is the shared fallback. If a password contains "$",
# escape it as "\$" so dotenv does not treat it as variable expansion.
SMTP_HOST=mail.naturerompsafaris.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_PASSWORD=

SMTP_ENQUIRY_USER=inquiries@naturerompsafaris.com
SMTP_ENQUIRY_PASSWORD=
SMTP_ENQUIRY_FROM=Nature Romp Safaris <inquiries@naturerompsafaris.com>
SMTP_GUEST_USER=no-reply@naturerompsafaris.com
SMTP_GUEST_PASSWORD=
SMTP_GUEST_FROM=Nature Romp Safaris <no-reply@naturerompsafaris.com>
SMTP_NEWSLETTER_USER=news@naturerompsafaris.com
SMTP_NEWSLETTER_PASSWORD=
SMTP_NEWSLETTER_FROM=Nature Romp Safaris <news@naturerompsafaris.com>

ENABLE_ENQUIRY_NOTIFICATIONS=true
ENQUIRY_NOTIFICATION_EMAIL=info@naturerompsafaris.com
# Optional CC on internal enquiry notifications (comma-separated).
ENQUIRY_CC_EMAIL=inquiries@naturerompsafaris.com
ENQUIRY_REPLY_TO=info@naturerompsafaris.com
ENABLE_GUEST_ENQUIRY_CONFIRMATION=true

# WhatsApp business number (NEXT_PUBLIC_ is used by the public floating button).
BRAND_WHATSAPP_PHONE=+254739206698
NEXT_PUBLIC_WHATSAPP_NUMBER=+254739206698
NEXT_PUBLIC_PHONE_PRIMARY=+254 739 206698
NEXT_PUBLIC_PHONE_OFFICE=+254 739 206698
NEXT_PUBLIC_BRAND_EMAIL=info@naturerompsafaris.com
WHATSAPP_API_ENABLED=false
WHATSAPP_NOTIFICATION_PHONE=+254739206698

# Portal self-signup allowlist (domains). Averis Systems is included for agency admins.
PORTAL_SIGNUP_ALLOWED_DOMAINS=naturerompsafaris.com,averissystems.com
